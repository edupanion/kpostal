name: webview_flutter_kpostal
description: Search Korean postal addresses with the Kakao postcode service, powered by webview_flutter.
version: 2.0.0
homepage: https://github.com/tykann/kpostal

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.13.1
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  assets:
    - assets/kakao_postcode.html
